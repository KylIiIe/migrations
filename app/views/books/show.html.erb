<h1><%= @book.name %></h1>

<p><strong>Published Date:</strong> <%= @book.year %></p>

<p><strong>Library:</strong> <%= @book.library.name %></p>

<h2>Authors</h2>
<ul>
  <% @authors.each do |author| %>
    <li><%= author.name %></li>
  <% end %>
</ul>

<h2>Genres</h2>
<ul>
  <% @genres.each do |genre| %>
    <li><%= genre.name %></li>
  <% end %>
</ul>

<%= link_to 'Усі книги', books_path %>
<%= link_to 'Редагувати', edit_book_path(@book) %>
<%= button_to "Видалити", @book, method: :delete %>

